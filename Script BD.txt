create table produto(
id int primary key generated always as identity,
nomeproduto varchar(280),
nomeextenso varchar(2000),
estrelas int,
status boolean,
quantidade int,
preco double
);

create table imagens(
id int primary key generated always as identity,
dir varchar(2000),
idproduto int
);
